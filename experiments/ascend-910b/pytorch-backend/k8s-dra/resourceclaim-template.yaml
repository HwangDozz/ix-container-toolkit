apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: ascend-gpu-claim
  namespace: crater-workspace
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: ascend-gpu
            allocationMode: ExactCount
            count: 1
